class Solution {
    int missingNum(int arr[]) {
        // code here
        long n=arr.length;
        long sum= (n+1)*(n+2)/2;
        long arraySum = 0;
        for(int i=0;i<n;i++)
        {
            arraySum+=arr[i];
        }
        return (int)(sum-arraySum);
    }
}
